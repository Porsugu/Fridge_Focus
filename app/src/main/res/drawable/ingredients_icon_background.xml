<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- White background -->
    <corners android:radius="32dp"/> <!-- Circle shape with a 32dp radius (adjust as needed) -->
    <stroke android:color="@color/black" android:width="1dp"/> <!-- Optional border -->
</shape>